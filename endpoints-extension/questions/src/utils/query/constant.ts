export enum Operator {
  EQUAL = '=',
  NOT_EQUAL = '<>',
  LESS_THAN_EQUAL = '<=',
  GREATER_THAN_EQUAL = '>=',
  LESS_THAN = '<',
  GREATER_THAN = '>',
  BETWEEN = 'BETWEEN',
  LIKE = 'LIKE',
  IN = 'IN',
}

export enum Summarize {
  SUM = 'SUM',
  COUNT = 'COUNT',
  AVERAGE = 'AVERAGE',
  MAX = 'MAX',
  MIN = 'MIN',
}
